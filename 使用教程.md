希望本产品对您有所帮助
# FPS 屏幕放大器 — 使用教程

## 快速开始

1. 双击 `FpsMagnifier.exe` 运行
2. 屏幕中央出现圆形放大区域（默认 **辅助模式**）
3. 系统托盘出现绿色放大镜图标

---

## 两种模式

### 🎮 辅助模式（默认）
放大镜固定在屏幕**正中央**，适合 FPS 射击游戏。
- 放大的是屏幕中心区域，用以辅助瞄准
- 显示**中心样式**（十字/圆点等）

### 🔍 普通放大镜
放大镜**跟随鼠标**移动，像真实放大镜一样。
- 移到想看的位置按 **`F8`** 锁定（停在原地持续监视）
- 再按 **`F8`** 解锁，继续跟随鼠标

> 在设置面板 → **通用** → **模式** 中切换

---

## 快捷键一览

| 按键 | 功能 | 适用模式 |
|------|------|---------|
| **`F6`** 或 **`Scroll Lock`** | 显示/隐藏 放大镜 | 通用 |
| **`Ctrl` + 鼠标滚轮** | 调**整放大镜大小** | 通用 |
| **`Alt` + 鼠标滚轮** | 调**整放大倍数**（1.5x~5x） | 通用 |
| **`F7`** | 打开**设置面板** | 通用 |
| **`F8`** | 锁定/解锁 位置 | 普通放大镜 |
| **`Ctrl` + `Shift` + `S`** | 快速保存当前配置为预设 | 通用 |

---

## 设置面板（按 F7）

### 通用
- **启用放大镜** — 开关
- **模式** — 辅助模式 / 普通放大镜

### 形状与尺寸
- **形状** — 圆形 / 正方形 / 长方形
  - 选"长方形"时可分别调**宽度**和**高度**
- **宽度** — 60~400px
- **高度** — 60~400px（仅长方形）
- **放大倍率** — 1.5x ~ 5.0x

### 外观
- **边框颜色** / **边框粗细** — 自定义边框样式
- **中心样式** / **中心颜色** — ⚠️ 仅在**辅助模式**下显示
  - 无 / 十字 / 圆点 / 十字+圆点

### 性能
- **极速 (120fps)** — 枪战游戏推荐
- **高质量 (60fps)** — 画质优先
- **节能 (30fps)** — 最低资源占用
- **自适应** — 默认 120fps

### 预设
- **保存为预设** — 把当前配置存为预设
- **加载预设** — 加载之前保存的配置（如"狙击4x"、"冲锋2x"）

---

## 右键托盘菜单

- **✓ 已启用 / ✗ 已禁用** — 开关放大镜
- **设置...** — 打开设置面板
- **退出** — 关闭程序

---

## 便携使用

把 `FpsMagnifier.exe` 复制到 **U 盘** 上：
- 任何 Windows 10/11 64 位电脑双击即用
- 配置自动保存在 exe 同级目录的 `config.json`
- 预设保存在 exe 同级目录的 `presets/` 文件夹

